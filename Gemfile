source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  # Only plugins NOT already in github-pages gem
end

gem "webrick" # needed for local serve in Ruby 3.x
